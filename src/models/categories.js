exports.get_categories = function(){
    /*models.Categories.create({ nom: "Correr", description: "algo"});
    models.Categories.findAll().then(categories => {
        console.log(categories);
        res.send(categories)});*/
    return ["Caminar", "Correr", "Senderisme", "Bicileta"];
};
